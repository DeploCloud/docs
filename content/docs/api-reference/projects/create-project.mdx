---
title: Create a project
description: "POST /v1/projects"
---

```
POST /v1/projects
```

<TypeTable
  type={{
    name: { description: "Project name, must be unique in your team", type: "string" },
    repository: { description: "GitHub repository to connect, owner/name", type: "string" },
    framework: { description: "Override auto-detection", type: "string", default: "auto-detected" },
  }}
/>

```bash
curl -X POST https://api.deplo.build/v1/projects \
  -H "Authorization: Bearer $DEPLO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "name": "marketing-site", "repository": "DeploCloud/marketing-site" }'
```
