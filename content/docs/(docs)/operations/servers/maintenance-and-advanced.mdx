---
title: Maintenance and advanced
description: Restarts, the host clock, the server's address, and removing a server.
---

import { RotateCcw, ArrowLeftRight, AlertTriangle } from "lucide-react";

**Settings -> Servers -> the server**, on the **Maintenance** and **Advanced** tabs.

## Maintenance

<TypeTable
  type={{
    "Restart Traefik": {
      type: "action",
      description:
        "Restarts the proxy on this host. Every site there is unreachable for the few seconds it takes to come back, and the containers keep running.",
    },
    "Restart all": {
      type: "action",
      description:
        "Restarts the workloads, apps and databases, one at a time, so each is briefly unreachable. Anything already stopped stays stopped, and anything mid-deploy is left to finish.",
    },
    "Restart Deplo": {
      type: "action",
      description:
        "Only on the host running the control plane. It restarts the panel itself, which goes away for a few seconds while your apps and databases keep serving traffic.",
    },
  }}
/>

## Advanced

<TypeTable
  type={{
    "What this server is for": {
      type: "read-only",
      description: "The role the machine was enrolled with. Beta, and it cannot be changed in place",
    },
    "Host details": {
      type: "read-only",
      description: "What the agent reports about the machine, including where Docker keeps its data",
    },
    "Server time": {
      type: "select",
      description:
        "The host's timezone, with Save timezone. Cron jobs carry their own zone, so this is for the host's own clock",
    },
    "Install command": {
      type: "text",
      description:
        "The command to re-run on the host, for reinstalling or repairing the agent. This is also where a fresh bootstrap token is issued",
    },
    "Agent port": {
      type: "number",
      default: "9443",
      description: "The port the control plane dials",
    },
    "Change address": {
      type: "text",
      description: "In the Danger zone. The address the control plane dials, for when the IP changes. Beta",
    },
    "Remove server": {
      type: "danger",
      description: "In the Danger zone. See below",
    },
  }}
/>

## Removing a server

**Removing is trust revocation, not an uninstall.** The pinned
certificate is cleared and Deplo never talks to that machine again. It
does not reach in and remove containers, because by then it may not be
able to.

Deplo prints a one-line uninstall command to run on the host if you want
the agent, its Traefik and its state directory gone. See
[Remove or uninstall](/operations/remove-a-server-or-uninstall).

The machine running the control plane cannot remove itself.

## Good to know

- **Health is a cache, not a gate.** Past a staleness window the page
  shows **Unknown** rather than a stale green. Deploys gate on a live
  handshake.
- **Restarting Traefik drops connections** for a moment on every app on
  that host.
- **Changing the address does not move anything.** It only changes how Deplo dials.

## Troubleshooting

| Symptom | What to check |
| --- | --- |
| The card says **warning** | The agent is up, Docker is not answering there. Check the Docker service on the host. |
| A restart action fails | The agent is unreachable. Health and readiness will say so. |

More in [Servers and agents](/troubleshooting/servers-and-agents).

## Next steps

<Cards>
  <Card
    icon={<RotateCcw className="text-blue-400" />}
    title="Server cleanup"
    description="Reclaiming disk"
    href="/operations/servers/cleanup"
  />
  <Card
    icon={<ArrowLeftRight className="text-blue-400" />}
    title="Upgrade"
    description="Agent updates"
    href="/operations/upgrade"
  />
  <Card
    icon={<AlertTriangle className="text-blue-400" />}
    title="Remove or uninstall"
    description="The three different acts"
    href="/operations/remove-a-server-or-uninstall"
  />
</Cards>
