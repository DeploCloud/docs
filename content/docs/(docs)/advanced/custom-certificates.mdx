---
title: Custom certificates
description: Install a TLS certificate you already hold, instead of letting Let's Encrypt issue one.
---

import { Globe, Server, LifeBuoy } from "lucide-react";

Use this for wildcards, a private certificate authority, or a hostname that
cannot complete an HTTP challenge.

**Settings -> Servers -> a server -> Certificates.** Instance admins only.

The certificate is installed into that host's Traefik store, and **nowhere
else**. Deplo keeps no copy: the list you see is read back off the host,
and the private key has no read path anywhere in the product.

A domain reaches it by setting its **Certificate** to **Installed on the
server**. That renders a router with TLS on and **no ACME resolver
named**, because naming one would make Traefik try to issue a certificate
for a hostname whose whole point is that it already has one.

A domain set to **Let's Encrypt** also picks it up, since Traefik will not issue for a name its
store already covers, and so does one set to **Cloudflare**, which resolves the same way. A domain
set to **None (no certificate)** never does: it is on plain HTTP by definition.

Installing a certificate that fully supersedes one already on the host replaces it; one that
merely overlaps is kept alongside.

## Install a certificate

<Steps>
<Step>

### Open Certificates

**Settings -> Servers**, choose the server, then **Certificates**.

</Step>
<Step>

### Add the pair

Add the certificate and its private key, both PEM.

</Step>
<Step>

### Point the domain at it

Set the app's domain **Certificate** to **Installed on the server**.

</Step>
<Step>

### Apply it

Redeploy the app, or use **Reload** to re-apply routing.

</Step>
</Steps>

**Refresh** re-reads the store from the host. **Remove** deletes it there.

## Important limits

- **Nothing renews it.** This is the big one. The page warns during the
  last three weeks, and after that visitors get an expiry error. Put the
  renewal in your own calendar.
- **It is per server.** An app running on two machines needs it on both.
- **Deplo cannot show you the key back.** Keep your own copy.
- **A wildcard is the usual reason to be here.** Let's Encrypt can issue
  wildcards, but only over a DNS challenge, which needs provider
  credentials in Traefik's static configuration. Installing the
  certificate yourself avoids that entirely.

## Troubleshooting

- **The browser still sees a Let's Encrypt certificate** - the domain is
  set to **Let's Encrypt** and Traefik already had one. Traefik prefers
  the more specific match; check the hostname really is covered by yours.
- **Traefik serves its own default certificate** - the pair did not load.
  Usually a missing intermediate in the chain, or a key that does not
  match the certificate.
- **It works on one app and not another** - the other app runs on a
  different server.

More in [Domains and TLS troubleshooting](/troubleshooting/domains-and-tls).

## See also

<Cards>
  <Card icon={<Globe className="text-blue-400" />} title="Domains and HTTPS" description="Routing and Let's Encrypt certificates" href="/guides/networking/domains-and-https" />
  <Card icon={<Server className="text-blue-400" />} title="Server settings" description="Per-server configuration" href="/operations/servers" />
  <Card icon={<LifeBuoy className="text-blue-400" />} title="Domains and TLS troubleshooting" description="Certificate and routing errors" href="/troubleshooting/domains-and-tls" />
</Cards>
