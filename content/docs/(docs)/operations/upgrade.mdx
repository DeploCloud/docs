---
title: Upgrade
description: Update the control plane and server agents, and know what changes on each side.
---

import { Download, Server, ShieldAlert, ShieldCheck, Settings } from "lucide-react";

Moving Deplo to a newer version. Two things version independently:

- The **control plane** (this panel)
- The **server agents**, one binary per host

- The control plane updates itself from the panel: the machine it runs on pulls the new image and restarts it. No shell, no SSH.
- Database migrations apply themselves at boot. There's no separate migration step.
- Agents update one server at a time, from the servers page.
- The control plane and the agents tolerate a version gap: new capabilities are announced in the handshake, so an older agent simply doesn't offer a newer feature rather than failing.

**Agent updates only ever move forward.** There is no downgrade path.

## Release history

**Settings -> Deplo -> Updates** is the panel's own changelog, so "what changed" has an answer without leaving Deplo.

- The version this instance runs, and whether a newer one is published.
- **Check now** re-asks the repository instead of waiting out the hourly cache.
- Every published release with its notes, newest first. The one you are running is marked **Installed**.
- How much of the fleet is on the agent release Deplo expects, with a link to the servers page.

A newer release also raises a banner across the dashboard and a dot on the **Deplo** entry in the sidebar. Dismissing the banner silences it for that version; the dot stays until you upgrade.

The list says what a release contains; the two sections below are how it gets applied.

## Upgrade the control plane

<Steps>
<Step>
### Open Settings -> Deplo -> Updates
</Step>

<Step>
### Click the **Update** button
It names the version it will install. Confirm, and the machine Deplo runs on pulls the
new image and restarts the panel.
</Step>

<Step>
### Wait about a minute
The page reloads itself when Deplo answers again on the new version.
</Step>
</Steps>

Three things happen around it that are worth knowing:

- A `pg_dump` of the panel's database lands in `/opt/deplo/backups` first, and the newest three are kept.
- **Secrets are never rotated**, and no setting is reset.
- If the new version does not come up, the previous one is put back automatically. The panel says
  so instead of leaving you on a spinner.

<Callout type="info" title="Your apps keep running throughout">
They are containers on hosts, not part of the control plane. A panel that's
down means you can't deploy or read logs for a minute, it does not mean your
sites are down.
</Callout>

### When Deplo cannot do it from the panel

The button asks the **server agent** on the machine to run the installer, so it needs that agent to
be recent enough. When it is not, or when Deplo is not running as a container the agent can see,
the panel says why and shows the command to run on the machine instead:

```bash
curl -fsSL https://deplo.build/install.sh | bash
```

It is the same script the button runs. It detects the existing `/opt/deplo/.env`, switches to update
mode, pulls the image and restarts, with the same dump and the same rollback.

To pin a version rather than take the newest:

```bash
curl -fsSL https://deplo.build/install.sh | \
  DEPLO_VERSION=0.1.0 bash
```

## Update the agents

<Steps>
<Step>
### Open Settings -> Servers
</Step>

<Step>
### Refresh
Click **Refresh**: it re-checks every agent and the latest agent release.
</Step>

<Step>
### Update each server that's behind
Open the server and click the **Update to** button on its **Overview** tab, which names the version it will install.
</Step>
</Steps>

Sensible order, especially on a fleet:

1. One canary server that runs something you can afford to break.
2. Watch it: readiness, a deploy, logs streaming.
3. The rest.
4. The machine running Deplo itself, last. If an agent update goes wrong there, you want the panel already proven on the new version.

Do not update an agent while a deploy is in flight on that host.

## Good to know

- **Agents cannot be downgraded.** Test on a canary.
- **Some features need a newer agent.** When one is missing, Deplo says so rather than silently doing something else.
- **Re-running the installer is safe and idempotent.** It is what the button does, so running it by hand is never a workaround.
- **Read the release notes for a minor bump.** During `0.x`, minor means something you notice: a feature, changed behavior, a database migration, or a newer agent required.
- **Back up before a big jump.** Migrations apply at boot and are not reversed by rolling the image back.

## Troubleshooting

Panel that won't come back after an upgrade, an image that won't pull, or an
agent update that fails: see
[Servers and agents troubleshooting](/troubleshooting/servers-and-agents).

## Next steps

<Cards>
  <Card icon={<Download className="text-blue-400" />} title="Installation" description="First-run setup on a fresh machine" href="/getting-started/install" />
  <Card icon={<Server className="text-blue-400" />} title="Server settings" description="Health, readiness, and the servers page" href="/operations/servers" />
  <Card icon={<ShieldCheck className="text-blue-400" />} title="Verify a release" description="Check the image was built by Deplo before you run it" href="/operations/verify-a-release" />
  <Card icon={<ShieldAlert className="text-blue-400" />} title="Disaster recovery" description="What to keep before anything breaks" href="/operations/disaster-recovery" />
  <Card icon={<Settings className="text-blue-400" />} title="Instance administration" description="Instance-wide settings and users" href="/operations/instance-administration" />
</Cards>
