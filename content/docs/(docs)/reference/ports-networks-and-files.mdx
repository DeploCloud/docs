---
title: Ports, networks, files
description: Everything Deplo listens on, connects over and writes to disk.
---

import { SlidersHorizontal, Terminal, Server, Network } from "lucide-react";

Use this page to check firewall rules, Docker networks, and host paths.

<Callout type="info" title="At a glance">
  Open **80 and 443** to the internet. Allow **9443** from the control plane
  to every server. Never publish **3000**, **5432** or **2375**.
</Callout>

## Ports

<TypeTable
  type={{
    "3000": {
      type: "The control plane",
      default: "bound to 127.0.0.1, never the server's address",
      description: "Nothing outside the machine reaches it. Traefik serves the dashboard over HTTPS on 443 instead.",
    },
    "80": {
      type: "Traefik, entry point web",
      default: "published on every server that routes",
      description: "Must be reachable from the internet, including for certificate challenges.",
    },
    "443": {
      type: "Traefik, entry point websecure",
      default: "published on every server that routes",
      description: "Must be reachable from the internet.",
    },
    "9443": {
      type: "deplo-agent, gRPC over mutual TLS",
      default: "published on every server",
      description: "Must be reachable from the control plane.",
    },
    "5432": {
      type: "Postgres",
      default: "never published",
      description: "Nothing reaches it. It is on an internal network.",
    },
    "2375": {
      type: "The Docker socket proxy",
      default: "never published",
      description: "Nothing reaches it but Traefik, on an internal network.",
    },
  }}
/>

<Callout type="warn" title="Common mistakes">
  - **Port 3000 is not open, on purpose.** A dashboard on an open port is
    a login page on the internet with no TLS in front of it. It binds
    `127.0.0.1`, so the way back in when the proxy is what broke is
    `ssh -L 3000:localhost:3000 root@your-server`, then
    `http://localhost:3000`. The generated `deplo-<hex>.nip.io` address
    covers everything short of that: it answers over HTTPS whatever your
    domain is doing.
  - **A green server card does not prove port 9443 is open.** The call
    home is outbound over 443, and that is what turns it green at
    enrolment. Opening the servers page re-dials every agent, so a
    blocked port shows up there as `offline` - but a card painted from
    an older reading still says online. No installer opens a firewall:
    the agent installer only tells you when `ufw` or `firewalld` is the
    one holding the port.
</Callout>

## Networks

These belong to the platform. Tenant stacks never join them:

<TypeTable
  type={{
    deplo: {
      type: "platform",
      description: "Traefik and the control plane. Not where apps go",
    },
    "deplo_deplo-internal": {
      type: "internal",
      description: "The control plane and its Postgres, and nothing else. Its compose key is deplo-internal",
    },
    "traefik_deplo-socket": {
      type: "internal",
      description: "Traefik and its read-only Docker socket proxy. Its compose key is deplo-socket",
    },
  }}
/>

Tenant networks are created per Environment:

<TypeTable
  type={{
    "deplo-env-<environment id>": {
      type: "tenant",
      description: "Every app and managed database placed in that Environment",
    },
    "deplo-team-<team id>": {
      type: "tenant",
      description: "Apps that are in no Environment: the team's top level, and anything in a folder",
    },
    "deplo-preview-<deploy key>": {
      type: "tenant",
      description: "One pull request preview, sealed on its own",
    },
  }}
/>

The separation prevents tenant containers from reaching platform services.
Deplo also reserves platform service names on managed networks to avoid
Docker DNS collisions.

### Address pools

Deplo uses one network per Environment. Both installers configure a `/13`
pool with `size: 24` in `/etc/docker/daemon.json`.

- An existing pool configuration is **never clobbered**.
- The previous file is kept as `daemon.json.deplo-bak`.
- The new one is validated with `dockerd --validate` before it is used.
- If containers are already running, Docker is not restarted automatically.

## Files on the panel's host

```
/opt/deplo/.env                     generated secrets, chmod 600
/opt/deplo/docker-compose.yml       Postgres + the control plane
/opt/deplo/traefik/                 the proxy stack
/opt/deplo/traefik/certs/           the fallback certificate, minted once
/opt/deplo/data/                    build and upload staging
/opt/deplo/acme/acme.json           issued certificates, chmod 600
```

## Files on every server

```
/usr/local/bin/deplo-agent          the agent binary
/var/lib/deplo-agent/               chmod 700
  agent.crt, agent.key, ca.crt      its mutual TLS identity
  bootstrap.env                     first-run enrollment, chmod 600
  traefik/                          that host's proxy stack
/etc/systemd/system/deplo-agent.service
/data/backups/                      the agent's managed backup store, chmod 700
/etc/docker/daemon.json             plus daemon.json.deplo-bak
```

On the control-plane host, `/var/lib/deplo-agent/traefik` is a **symlink** to
`/opt/deplo/traefik`, so the agent manages the panel's proxy.

`/var/lib/deplo-agent` holds Traefik's `acme.json`. Deleting it takes
your issued certificates with it, and Let's Encrypt rate-limits
reissuance.

## Common container names

<TypeTable
  type={{
    "deplo-traefik": {
      type: "proxy",
      description: "The proxy. Deplo identifies it by this name",
    },
    "deplo-deplo-1": {
      type: "control plane",
      description: "The panel container on the host that runs it. Compose names it, so there is no container_name",
    },
    "deplo-postgres-1": {
      type: "control plane",
      description: "The control plane's own Postgres",
    },
    "traefik-deplo-socket-proxy-1": {
      type: "proxy",
      description: "The read-only Docker socket proxy Traefik reads events through",
    },
    "deplo-<slug>": {
      type: "app stack",
      description: "An app's stack.",
    },
    "deplo-<slug>__pr-<n>": {
      type: "preview stack",
      description: "A pull request preview.",
    },
    "db-<name>": {
      type: "database",
      description: "A managed database.",
    },
    "deplo-<slug>-<volume>": {
      type: "volume",
      description: "An app's named volume.",
    },
  }}
/>

## See also

<Cards>
  <Card
    icon={<SlidersHorizontal className="text-blue-400" />}
    title="Platform variables"
    description="Every variable Deplo itself reads"
    href="/reference/environment-variables"
  />
  <Card
    icon={<Terminal className="text-blue-400" />}
    title="Installers reference"
    description="What writes these files"
    href="/reference/installers"
  />
  <Card
    icon={<Server className="text-blue-400" />}
    title="Add a server"
    description="Enrolling a host"
    href="/operations/servers/add-a-server"
  />
  <Card
    icon={<Network className="text-blue-400" />}
    title="How Deplo works"
    description="The control plane and the agent"
    href="/concepts/how-deplo-works"
  />
</Cards>
