---
title: Container registries
description: Credentials for pulling private images, from GitHub Container Registry, Docker Hub, GitLab or anything self-hosted.
---

import { Container, Hammer, ShieldCheck } from "lucide-react";
import { TabsList, TabsTrigger } from "fumadocs-ui/components/tabs";
import { siGithub, siDocker, siGitlab } from "simple-icons";

**Settings -> Registries**.

<Callout type="info" title="Beta">
This feature is currently beta/experimental and subject to change. It is
not recommended for production. Try it out and share your feedback on
[our GitHub](https://github.com/DeploCloud/deplo/issues).
</Callout>

---

## How it works

One entry per registry, stored per team and encrypted. On deploy, the
agent uses it to pull. The image autocomplete in the app wizard also
starts including that registry, so private images become searchable
rather than something you have to type exactly.

The password or token is write-only: it is never returned by the API and
there is no reveal path.

The credential itself is not something Deplo issues, it is created on
the registry provider's own site first, then pasted in here.

---

## Add one

<Steps>
<Step>

### Open registries settings

**Settings -> Registries**, then **Add registry**.

</Step>
<Step>

### Pick the type

Pick the **Type**: `ghcr`, `dockerhub`, `gitlab` or `generic`.

</Step>
<Step>

### Create a credential on the provider, then fill in the form

Go to the provider's own site first and create a read-only credential.
The exact steps, and what goes in **Registry host**, **Username** and
**Token**, differ per provider:

<Tabs defaultValue="ghcr">
<TabsList>
<TabsTrigger value="ghcr">
  {/* the page background is always black (Deplo has no light theme), so
      GitHub's own dark-surface mark (white) is the true logo here, not its
      light-surface black */}
  <svg role="img" viewBox="0 0 24 24" fill="#ffffff" aria-hidden="true">
    <path d={siGithub.path} />
  </svg>
  GHCR (GitHub)
</TabsTrigger>
<TabsTrigger value="dockerhub">
  <svg role="img" viewBox="0 0 24 24" fill={`#${siDocker.hex}`} aria-hidden="true">
    <path d={siDocker.path} />
  </svg>
  Docker Hub
</TabsTrigger>
<TabsTrigger value="gitlab">
  <svg role="img" viewBox="0 0 24 24" fill={`#${siGitlab.hex}`} aria-hidden="true">
    <path d={siGitlab.path} />
  </svg>
  GitLab
</TabsTrigger>
<TabsTrigger value="generic">
  <Container />
  Generic
</TabsTrigger>
</TabsList>
<Tab value="ghcr">

- **Registry host**: `ghcr.io`.
- **Username**: your GitHub username.
- **Token**: a **classic** personal access token with the `read:packages`
  scope, created under GitHub's own **Settings -> Developer settings ->
  Personal access tokens -> Tokens (classic)**. If the image belongs to an
  organization, the org may also need to grant that token access under its
  package settings.

<Callout type="warn" title="Classic token, not fine-grained">
GHCR does not work with a **fine-grained** personal access token: GitHub's
fine-grained tokens have no `packages` permission to grant, so a pull with
one fails with `unauthorized` even if the token looks valid. Use **Tokens
(classic)** and tick `read:packages`.
</Callout>

See GitHub's docs on
[authenticating to the Container registry](https://docs.github.com/en/packages/working-with-a-github-packages-registry/working-with-the-container-registry)
for the exact steps.

</Tab>
<Tab value="dockerhub">

- **Registry host**: `docker.io`.
- **Username**: your Docker Hub username.
- **Token**: a Docker Hub access token, not your account password.
  Create one on Docker Hub under **Account settings -> Personal access
  tokens**, with **Read** permission.

See Docker's docs on
[personal access tokens](https://docs.docker.com/security/access-tokens/)
for the exact steps.

</Tab>
<Tab value="gitlab">

- **Registry host**: `registry.gitlab.com`, or your self-hosted
  instance's registry host.
- **Username**: the deploy token's username, shown by GitLab when the
  token is created, not your account name.
- **Token**: a GitLab deploy token with the `read_registry` scope,
  created under the project or group's **Settings -> Repository ->
  Deploy tokens**.

See GitLab's docs on
[deploy tokens](https://docs.gitlab.com/ee/user/project/deploy_tokens/)
for the exact steps.

</Tab>
<Tab value="generic">

For anything else, self-hosted Harbor, a cloud provider's container
registry, or another self-hosted registry, use whatever credential that
registry issues for read-only pulls, for example a robot account or an
IAM-generated token. There is no common format across self-hosted
registries, check that provider's own documentation for how to create
one.

- **Registry host**: the registry's own host.
- **Username** and **Token**: whatever that registry issued.

</Tab>
</Tabs>

</Step>
<Step>

### Save

Save.

</Step>
</Steps>

Use it by referencing the image in an app whose source is
[a Docker image](/guides/deploy/docker-image), or as the base image of
a Dockerfile build.

<Callout type="warn" title="Limits and gotchas">
- **Registries are per team.** Another team needs its own entry.
- **Tokens expire.** A pull that used to work and now fails with
  `unauthorized` is almost always this.
- **Scope the token to read-only** on the provider's side. Deplo only
  ever pulls.
- **Deplo pushes nothing.** Images it builds stay on the server that
  built them, which is why [rollbacks](/guides/releases/rollbacks) depend
  on local retention rather than a registry.
</Callout>

<Callout type="error" title="If it does not work">
- **`pull access denied`**: no credential matches that image's host, or
  the token cannot see that repository.
- **It works for one image and not another**: same host, different
  repository permissions on the provider's side.
- **A self-hosted registry with a private address is refused**: only an
  instance admin can allow a non-public endpoint.

See [Deploys and builds](/troubleshooting/deploys-and-builds) for more.
</Callout>

---

## See also

<Cards>
  <Card
    icon={<Container className="text-blue-400" />}
    title="Deploy a Docker image"
    description="Reference a private image directly"
    href="/guides/deploy/docker-image"
  />
  <Card
    icon={<Hammer className="text-blue-400" />}
    title="Build settings"
    description="Use a registry as a Dockerfile base image"
    href="/guides/releases/build-settings"
  />
  <Card
    icon={<ShieldCheck className="text-blue-400" />}
    title="Capabilities"
    description="manage_registries"
    href="/reference/capabilities"
  />
</Cards>
